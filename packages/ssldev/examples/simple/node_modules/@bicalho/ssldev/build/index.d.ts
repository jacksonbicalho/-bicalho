import type { ServerConfigType } from "./server";
declare class SslDev {
    private configServer;
    constructor(configServer: ServerConfigType);
    configure(configServer: ServerConfigType): this;
    run(): void;
}
declare const sslldev: SslDev;
export { sslldev, ServerConfigType };
